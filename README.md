# money-manager
Money manager

Проєкт: “Менеджер покупок”

Консольна програма, яка допомагає користувачу планувати покупки, рахувати суму, зберігати дані у файл і завантажувати їх при повторному запуску.

---

## 📌 Можливості програми

Програма дозволяє:

### ➕ 1. Додавати новий товар  
Користувач вводить:
- назву товару  
- кількість товару 
- ціну за одиницю  

Товар зберігається у вигляді словника:

```python
{
    "name": "Milk",
    "quantity": 2,
    "price": 1.5
}
📄 2. Переглядати весь список покупок
Список виводиться у зручному форматі:

markdown
Copy code
1. Milk – 2 x 1.50$
2. Apples – 5 x 0.80$
💰 3. Розраховувати загальну суму
Функція обчислює суму всіх покупок:

powershell
Copy code
Total price: 12.30$
💾 4. Зберігати список у файл
Список зберігається у файл text.txt у вигляді:

markdown
Copy code
1. Milk - 2 X 1.5$
2. Bread - 1 X 1.2$
📂 5. Завантажувати список із файлу
Функція load_from_file() зчитує файл назад у структуру даних Python.

🚪 6. Вихід
Завершення роботи програми.

🧩 Структура проєкту
add_item() — додає товар у список

show_list() — показує список

count_total() — рахує загальну суму товару

save_to_file() — зберігає список у text.txt

load_from_file() — завантажує список із файлу

main() — головне меню та логіка програми

🧪 Обробка помилок
Програма враховує:

неправильний ввід під час вибору меню

неправильний тип даних (наприклад, замість числа вводиться текст)

відсутність файлу при завантаженні

🎯 Мета проєкту
Навчання роботи з:

структурами даних (dict, list)

обробкою виключень (try/except)

файлами (open, читання, запис)

циклом while True

структурою match/case



